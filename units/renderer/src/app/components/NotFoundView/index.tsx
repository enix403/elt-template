import { NavPageView } from '~/app/layout/views';

export const NotFoundView = () => {
    return (
        <NavPageView title="Not found">
            <p style={{ zIndex: 100 }}>Page Not found</p>
        </NavPageView>
    );
};



